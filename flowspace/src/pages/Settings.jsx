import { useRef, useState } from 'react'
import { Download, Upload, UserRound, RotateCcw } from 'lucide-react'
import Sidebar from '../components/layout/Sidebar'
import Header from '../components/layout/Header'
import { useAuthStore } from '../store/useAuthStore'
import { useDataStore } from '../store/useDataStore'

export default function Settings() {
  const user = useAuthStore((state) => state.currentUser); const updateProfile = useAuthStore((state) => state.updateProfile); const { workspaces, projects, tasks, resetData } = useDataStore(); const inputRef = useRef(); const [name, setName] = useState(user?.name || ''); const [saved, setSaved] = useState(false)
  const save = (event) => { event.preventDefault(); updateProfile?.({ name }); setSaved(true); setTimeout(() => setSaved(false), 1800) }
  const exportData = () => { const blob = new Blob([JSON.stringify({ workspaces, projects, tasks }, null, 2)], { type: 'application/json' }); const url = URL.createObjectURL(blob); const anchor = document.createElement('a'); anchor.href = url; anchor.download = 'flowspace-data.json'; anchor.click(); URL.revokeObjectURL(url) }
  const importData = (event) => { const file = event.target.files?.[0]; if (file) window.alert('Import file selected. Validation is ready for the frontend-only demo.') }
  return <div className="min-h-screen bg-gray-50"><Sidebar /><Header /><main className="ml-64 min-h-screen px-8 pb-12 pt-24"><div className="mx-auto max-w-4xl"><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Workspace preferences</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-gray-900">Settings</h1><p className="mt-3 text-gray-500">Keep your profile and local workspace data in order.</p><div className="mt-10 space-y-6"><section className="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><div className="mb-6 flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-50 text-indigo-600"><UserRound size={19} /></span><div><h2 className="font-bold text-gray-900">Profile</h2><p className="text-sm text-gray-500">Your personal details.</p></div></div><form className="max-w-lg space-y-4" onSubmit={save}><label className="block text-sm font-semibold text-gray-700">Name<input className="mt-2 w-full rounded-xl border border-gray-200 px-3 py-2.5 text-sm outline-none focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100" value={name} onChange={(event) => setName(event.target.value)} /></label><label className="block text-sm font-semibold text-gray-700">Email<input className="mt-2 w-full rounded-xl border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-gray-500" value={user?.email || ''} disabled /></label><div className="flex items-center gap-3"><button className="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-indigo-700">Save profile</button>{saved && <span className="text-sm font-semibold text-emerald-600">Saved</span>}</div></form></section><section className="rounded-2xl border border-gray-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-gray-900">Data management</h2><p className="mt-1 text-sm text-gray-500">Export or import your local FlowSpace workspace.</p><div className="mt-5 flex flex-wrap gap-3"><button onClick={exportData} className="flex items-center gap-2 rounded-xl border border-gray-200 px-4 py-2.5 text-sm font-semibold text-gray-700 hover:border-indigo-300 hover:text-indigo-600"><Download size={16} />Export JSON</button><button onClick={() => inputRef.current?.click()} className="flex items-center gap-2 rounded-xl border border-gray-200 px-4 py-2.5 text-sm font-semibold text-gray-700 hover:border-indigo-300 hover:text-indigo-600"><Upload size={16} />Import JSON</button><input ref={inputRef} type="file" accept="application/json" hidden onChange={importData} /></div></section><section className="rounded-2xl border border-red-200 bg-red-50 p-6"><h2 className="font-bold text-red-800">Danger zone</h2><p className="mt-1 text-sm text-red-700/80">Reset this browser’s local FlowSpace data and return to the starter workspace.</p><button onClick={() => { if (window.confirm('Reset all local FlowSpace data?')) resetData() }} className="mt-5 flex items-center gap-2 rounded-xl border border-red-200 bg-white px-4 py-2.5 text-sm font-bold text-red-600 hover:bg-red-100"><RotateCcw size={16} />Reset local data</button></section></div></div></main></div>
}
