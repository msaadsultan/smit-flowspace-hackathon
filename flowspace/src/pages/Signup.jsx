import { useState } from 'react'
import { ArrowRight, CheckCircle2, LockKeyhole, Mail, Rocket, UserRound } from 'lucide-react'
import { Link, Navigate, useNavigate } from 'react-router-dom'
import { useAuthStore } from '../store/useAuthStore'

export default function Signup() {
  const navigate = useNavigate(); const { signup, isLoggedIn } = useAuthStore(); const [form, setForm] = useState({ name: '', email: '', password: '' }); const [error, setError] = useState(''); const [loading, setLoading] = useState(false)
  if (isLoggedIn) return <Navigate to="/dashboard" replace />
  const submit = async (event) => { event.preventDefault(); if (form.name.trim().length < 2 || form.password.length < 6) return setError('Use your name and a password with at least 6 characters.'); setLoading(true); const result = await signup(form.name.trim(), form.email, form.password); if (result.success) navigate('/dashboard'); else setError(result.error); setLoading(false) }
  return <main className="min-h-screen bg-white lg:grid lg:grid-cols-2"><section className="relative hidden min-h-screen overflow-hidden bg-gradient-to-br from-indigo-600 via-indigo-700 to-purple-800 p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="relative"><div className="flex items-center gap-3 text-xl font-bold tracking-tight"><span className="grid h-10 w-10 place-items-center rounded-xl bg-white/15"><Rocket size={21} /></span>FlowSpace</div><div className="mt-32 max-w-xl"><p className="mb-5 text-sm font-semibold uppercase tracking-[.22em] text-indigo-200">Make room for momentum</p><h1 className="text-5xl font-bold leading-[1.05] tracking-tight">A better home for good work.</h1><p className="mt-6 max-w-md text-lg leading-8 text-indigo-100">Set up a focused workspace and make the next step obvious.</p><div className="mt-10 space-y-4">{['Invite your whole team', 'See work the way you think', 'Keep every decision close'].map((item) => <div className="flex items-center gap-3 text-sm text-indigo-100" key={item}><CheckCircle2 size={18} className="text-indigo-200" />{item}</div>)}</div></div></div></section><section className="flex min-h-screen items-center justify-center px-6 py-12 sm:px-10"><div className="w-full max-w-md"><div className="mb-12 flex items-center gap-3 text-xl font-bold tracking-tight text-gray-900 lg:hidden"><span className="grid h-10 w-10 place-items-center rounded-xl bg-indigo-600 text-white"><Rocket size={21} /></span>FlowSpace</div><div className="mb-10"><p className="text-sm font-semibold uppercase tracking-[.18em] text-indigo-600">Start fresh</p><h2 className="mt-3 text-3xl font-bold tracking-tight text-gray-900">Create your workspace</h2><p className="mt-3 text-gray-500">A clear place for your team to make progress.</p></div><form className="space-y-5" onSubmit={submit}><label className="block text-sm font-semibold text-gray-700">Your name<div className="relative mt-2"><UserRound size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input className="w-full rounded-xl border border-gray-200 bg-gray-50 py-3 pl-10 pr-4 text-sm outline-none transition focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-100" required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} placeholder="Alex Morgan" /></div></label><label className="block text-sm font-semibold text-gray-700">Email address<div className="relative mt-2"><Mail size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input className="w-full rounded-xl border border-gray-200 bg-gray-50 py-3 pl-10 pr-4 text-sm outline-none transition focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-100" type="email" required value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} placeholder="alex@company.com" /></div></label><label className="block text-sm font-semibold text-gray-700">Password<div className="relative mt-2"><LockKeyhole size={17} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input className="w-full rounded-xl border border-gray-200 bg-gray-50 py-3 pl-10 pr-4 text-sm outline-none transition focus:border-indigo-500 focus:bg-white focus:ring-4 focus:ring-indigo-100" type="password" required value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} placeholder="At least 6 characters" /></div></label>{error && <p className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-600">{error}</p>}<button disabled={loading} className="flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-600 py-3.5 text-sm font-bold text-white shadow-lg shadow-indigo-200 transition hover:bg-indigo-700 disabled:opacity-70">{loading ? 'Creating workspace...' : 'Create workspace'}{!loading && <ArrowRight size={17} />}</button></form><p className="mt-8 text-center text-sm text-gray-500">Already have an account? <Link className="font-bold text-indigo-600 hover:text-indigo-700" to="/login">Sign in</Link></p></div></section></main>
}
