import { useState } from 'react'
import { CalendarDays, List, Plus, Search, TableProperties } from 'lucide-react'
import { Link, useParams } from 'react-router-dom'
import Sidebar from '../components/layout/Sidebar'
import Header from '../components/layout/Header'
import KanbanBoard from '../components/kanban/KanbanBoard'
import TaskModal from '../components/tasks/TaskModal'
import { useAuthStore } from '../store/useAuthStore'
import { useDataStore } from '../store/useDataStore'

export default function ProjectDetail() {
  const { projectId } = useParams(); const { projects, tasks, moveTask } = useDataStore(); const users = useAuthStore((state) => state.users); const project = projects.find((item) => item.id === projectId); const [view, setView] = useState('kanban'); const [query, setQuery] = useState(''); const [modalOpen, setModalOpen] = useState(false); const [editing, setEditing] = useState(null)
  if (!project) return <div className="p-10"><Link className="text-indigo-600" to="/dashboard">Back to dashboard</Link></div>
  const projectTasks = tasks.filter((task) => task.projectId === projectId && `${task.title} ${task.description}`.toLowerCase().includes(query.toLowerCase()))
  return <div className="min-h-screen bg-gray-50"><Sidebar /><Header /><main className="ml-64 min-h-screen px-8 pb-12 pt-24"><div className="mx-auto max-w-7xl"><div className="mb-7 flex items-center gap-2 text-sm text-gray-400"><Link className="hover:text-indigo-600" to="/dashboard">Overview</Link><span>/</span><span>{project.name}</span></div><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Project workspace</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-gray-900">{project.name}</h1><p className="mt-2 max-w-xl text-sm text-gray-500">{project.description}</p></div><button className="flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-4 py-3 text-sm font-bold text-white shadow-lg shadow-indigo-200 hover:bg-indigo-700" onClick={() => { setEditing(null); setModalOpen(true) }}><Plus size={17} />Add task</button></div><div className="mt-9 flex flex-col justify-between gap-4 border-b border-gray-200 pb-3 sm:flex-row sm:items-center"><div className="flex gap-1 rounded-xl bg-gray-100 p-1">{[{ id: 'kanban', label: 'Kanban', icon: TableProperties }, { id: 'list', label: 'List', icon: List }, { id: 'calendar', label: 'Calendar', icon: CalendarDays }].map(({ id, label, icon: Icon }) => <button key={id} className={`flex items-center gap-2 rounded-lg px-3 py-2 text-xs font-semibold ${view === id ? 'bg-white text-indigo-600 shadow-sm' : 'text-gray-500 hover:text-gray-900'}`} onClick={() => setView(id)}><Icon size={15} />{label}</button>)}</div><div className="relative w-full sm:w-64"><Search size={15} className="absolute left-3 top-1/2 -translate-y-1/2 text-gray-400" /><input className="w-full rounded-xl border border-gray-200 bg-white py-2.5 pl-9 pr-3 text-sm outline-none focus:border-indigo-400 focus:ring-4 focus:ring-indigo-50" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Filter tasks" /></div></div><div className="mt-7">{view === 'kanban' && <KanbanBoard tasks={projectTasks} users={users} onMove={moveTask} onEdit={(task) => { setEditing(task); setModalOpen(true) }} />}{view === 'list' && <div className="overflow-x-auto rounded-2xl border border-gray-200 bg-white shadow-sm"><div className="grid min-w-[650px] grid-cols-[2fr_1fr_1fr_1fr] gap-4 border-b border-gray-100 px-5 py-3 text-xs font-bold uppercase tracking-wider text-gray-400"><span>Task</span><span>Status</span><span>Priority</span><span>Due</span></div>{projectTasks.map((task) => <button key={task.id} className="grid min-w-[650px] w-full grid-cols-[2fr_1fr_1fr_1fr] gap-4 border-b border-gray-100 px-5 py-4 text-left text-sm hover:bg-gray-50" onClick={() => { setEditing(task); setModalOpen(true) }}><span className="font-semibold text-gray-900">{task.title}</span><span className="text-gray-500">{task.status}</span><span className="text-gray-500">{task.priority}</span><span className="text-gray-500">{task.dueDate || '—'}</span></button>)}</div>}{view === 'calendar' && <div className="rounded-2xl border border-gray-200 bg-white p-5 shadow-sm"><h2 className="mb-5 text-lg font-bold text-gray-900">September 2026</h2><div className="grid grid-cols-7 gap-px overflow-hidden rounded-xl bg-gray-200">{['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'].map((day) => <div key={day} className="bg-gray-50 p-3 text-xs font-bold text-gray-400">{day}</div>)}{Array.from({ length: 30 }, (_, index) => { const date = `2026-09-${String(index + 1).padStart(2, '0')}`; return <div key={date} className="min-h-24 bg-white p-2"><span className="text-xs text-gray-400">{index + 1}</span>{projectTasks.filter((task) => task.dueDate === date).map((task) => <button key={task.id} className="mt-2 block w-full truncate rounded-md bg-indigo-50 px-2 py-1 text-left text-[10px] font-semibold text-indigo-700" onClick={() => { setEditing(task); setModalOpen(true) }}>{task.title}</button>)}</div>})}</div></div>}</div></div></main><TaskModal open={modalOpen} task={editing} projectId={projectId} onClose={() => { setModalOpen(false); setEditing(null) }} /></div>
}
